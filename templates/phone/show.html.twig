{% extends 'base.html.twig' %}

{% block title %}Phone{% endblock %}

{% block body %}
    <h1>Phone</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ phone.id }}</td>
            </tr>
            <tr>
                <th>IsBlocked</th>
                <td>{{ phone.isBlocked ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Picture</th>
                <td>{{ phone.picture }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_phone_index') }}">back to list</a>

    <a href="{{ path('app_phone_edit', {'id': phone.id}) }}">edit</a>

    {{ include('phone/_delete_form.html.twig') }}
{% endblock %}
